[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "PYTHONPATH=/app/backend python -m uvicorn app:app --host 0.0.0.0 --port ${PORT:-8000}"
